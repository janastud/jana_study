<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Jana Study — Казахский без зубрёжки</title>
  <meta name="description" content="Онлайн-уроки казахского языка для детей и взрослых. Персонально, удобно, с ИИ-ботом для проверки домашки." />
  <meta property="og:title" content="Jana Study — Казахский без зубрёжки" />
  <meta property="og:description" content="Онлайн-уроки казахского языка для детей и взрослых. Персонально, удобно, с ИИ-ботом для проверки домашки." />
  <meta property="og:type" content="website" />
  <meta property="og:image" content="/assets/og.jpg" />
  <meta name="theme-color" content="#365b6d" />

  <style>
    :root {
      --brand-primary: #365b6d;
      --brand-secondary: #6c9286;
      --brand-dark: #365b6d;
      --brand-light: #f2f1ec;
      --brand-gradient: linear-gradient(135deg, #6c9286, #365b6d);

      --ui-text: #0b1020;
      --ui-muted: #5b6470;
      --ui-border: #e8edf3;
      --ui-bg: #ffffff;
      --radius-xl: 20px;
      --radius-lg: 14px;
      --radius-md: 10px;
      --shadow-sm: 0 6px 25px rgba(16, 24, 40, 0.08);
    }

    * { box-sizing: border-box; }
    html, body { height: 100%; }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
      color: var(--ui-text);
      background: var(--brand-light);
      line-height: 1.55;
    }

    .container { width: min(1120px, 92vw); margin: 0 auto; }
    .btn {
      display: inline-flex; align-items: center; gap: .6rem;
      padding: .9rem 1.2rem; border-radius: var(--radius-lg);
      border: 1px solid transparent; text-decoration: none; font-weight: 600;
      transition: .2s ease; cursor: pointer;
    }
    .btn-primary { background: #6c9286; color: white; box-shadow: var(--shadow-sm); border-color: #6c9286; }
    .btn-primary:hover { transform: translateY(-1px); opacity: .95; filter: brightness(0.98); }
    .btn-ghost { background: transparent; border-color: var(--ui-border); color: var(--ui-text); }
    .btn-ghost:hover { border-color: var(--brand-primary); color: var(--brand-primary); }

    /* ========================= HEADER ========================= */
    header {
      position: sticky; top: 0; z-index: 20;
      background: rgba(255,255,255,.8); backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--ui-border);
    }
    .nav { display: flex; align-items: center; justify-content: space-between; padding: 14px 0; }
    .brand { display: flex; align-items: center; gap: .8rem; text-decoration: none; color: inherit; }
    .brand img { width: 40px; height: 40px; object-fit: contain; }
    .brand .title { font-size: 1.05rem; font-weight: 800; letter-spacing: .2px; }

    .navlinks { display: flex; align-items: center; gap: 1rem; }
    /* убрали дефолтные стили ссылок, чтобы классы .btn выглядели 1-в-1 как остальные кнопки */

    .mobile-toggle { display: none; background: transparent; border: none; font-size: 1.4rem; }

    @media (max-width: 860px) {
      .navlinks { display: none; }
      .mobile-toggle { display: inline-flex; }
      .mobile-menu { display: none; position: absolute; left: 0; right: 0; top: 62px; background: white; border-bottom: 1px solid var(--ui-border); }
      .mobile-menu a { display: block; padding: 14px 24px; text-decoration: none; color: var(--ui-text); border-top: 1px solid var(--ui-border); }
      .mobile-menu a:hover { background: var(--brand-light); }
      .mobile-menu.open { display: block; }
    }

    /* ========================= HERO ========================= */
    .hero { padding: 60px 0 30px; }
    .hero-card {
      background: var(--ui-bg); border: 1px solid var(--ui-border);
      box-shadow: var(--shadow-sm); border-radius: var(--radius-xl);
      display: grid; grid-template-columns: 1.25fr 1fr; gap: 28px; padding: clamp(18px, 3vw, 28px);
    }
    .hero h1 { font-size: clamp(28px, 3.2vw, 44px); line-height: 1.12; margin: 8px 0 10px; }
    .hero p.lead { font-size: clamp(16px, 1.4vw, 18px); color: var(--ui-muted); margin: 0 0 18px; }
    .badges { display: flex; flex-wrap: wrap; gap: 10px; margin: 14px 0 18px; }
    .badge { font-size: 13px; padding: 8px 10px; border: 0; border-radius: 999px; background: var(--brand-primary); color: #ffffff; }

    .hero-visual { position: relative; }
    .zhanne {
      width: 100%; aspect-ratio: 1/1; object-fit: contain; border-radius: var(--radius-lg);
      background: var(--brand-light); border: 1px dashed #cfe6f7;
    }
    .label {
      position: absolute; bottom: -10px; left: 10px; padding: 8px 12px; font-size: 13px; border-radius: 999px;
      background: var(--brand-dark); color: white; box-shadow: var(--shadow-sm);
    }

    @media (max-width: 980px) { .hero-card { grid-template-columns: 1fr; } }

    /* ========================= FEATURES ========================= */
    .section { padding: 42px 0; }
    .section h2 { font-size: clamp(22px, 2.6vw, 32px); margin: 0 0 18px; }
    .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
    @media (max-width: 960px) { .grid-3 { grid-template-columns: 1fr; } }

    .card {
      background: var(--ui-bg); border: 1px solid var(--ui-border); border-radius: var(--radius-lg);
      padding: 18px; box-shadow: var(--shadow-sm);
    }
    .icon {
      width: 40px; height: 40px; border-radius: 10px; display: inline-grid; place-items: center;
      background: var(--brand-light); margin-bottom: 10px;
    }

    /* ========================= CTA STRIP ========================= */
    .cta-strip { margin: 24px 0; border-radius: var(--radius-xl); background: var(--brand-dark); color: white; padding: 22px; display: grid; grid-template-columns: 1fr auto; align-items: center; gap: 14px; }
    .cta-strip strong { font-size: clamp(18px, 2.2vw, 24px); }
    @media (max-width: 760px) { .cta-strip { grid-template-columns: 1fr; } }

    /* ========================= PRICING ========================= */
    .grid-2 { display: grid; grid-template-columns: repeat(2, 1fr); gap: 18px; }
    @media (max-width: 960px) { .grid-2 { grid-template-columns: 1fr; } }

    /* ========================= FAQ ========================= */
    details { background: var(--ui-bg); border: 1px solid var(--ui-border); border-radius: var(--radius-lg); padding: 14px 16px; }
    details + details { margin-top: 10px; }
    summary { cursor: pointer; font-weight: 600; }
    summary::marker { color: var(--brand-primary); }

    /* ========================= FOOTER ========================= */
    footer { margin-top: 40px; padding: 30px 0 44px; border-top: 1px solid var(--ui-border); background: #fff; }
    .foot { display: grid; grid-template-columns: 1fr auto; gap: 10px; align-items: center; }
    @media (max-width: 760px) { .foot { grid-template-columns: 1fr; } }
    .foot a { color: inherit; text-decoration: none; }
    .foot a:hover { color: var(--brand-primary); }

    /* small helpers */
    .muted { color: var(--ui-muted); }
    .pill { padding: 6px 10px; background: var(--brand-primary); color: #fff; border-radius: 999px; font-size: 12px; }
    .spacer { height: 16px; }
      /* ===== Extra mobile tweaks and gallery ===== */
    @media (max-width: 600px) {
      .container { width: min(1120px, 94vw); }
      .hero h1 { font-size: 26px; }
      .hero p.lead { font-size: 15px; }
      .hero .btn { width: 100%; justify-content: center; }
      .badge { font-size: 12px; }
    }
    .gallery { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
    .gallery img { width: 100%; height: 220px; object-fit: cover; border-radius: var(--radius-lg); border: 1px solid var(--ui-border); box-shadow: var(--shadow-sm); }
    @media (max-width: 960px) { .gallery { grid-template-columns: 1fr 1fr; } }
    @media (max-width: 600px) { .gallery { grid-template-columns: 1fr; } .gallery img { height: 220px; } }
  </style>
</head>
<body>
  <!-- ========================= HEADER ========================= -->
  <header>
    <div class="container nav" role="navigation" aria-label="Main">
      <a class="brand" href="#">
        <picture>
          <source srcset="./assets/jana_study_logo-removebg-preview.png" type="image/png" />
          <img src="./assets/jana-study-logo.png" alt="Логотип Jana Study" width="40" height="40" />
        </picture>
        <span class="title">Jana Study</span>
      </a>

      <nav class="navlinks" aria-label="Primary">
        <a class="btn btn-ghost" href="#about">О школе</a>
        <a class="btn btn-ghost" href="#features">Преимущества</a>
        <a class="btn btn-ghost" href="#faq">FAQ</a>
        <a class="btn btn-ghost" href="#cta">Записаться</a>
      </nav>

      <button class="mobile-toggle" aria-label="Меню" onclick="document.querySelector('.mobile-menu').classList.toggle('open')">☰</button>
    </div>
    <div class="mobile-menu container" aria-label="Mobile Menu">
      <a href="#about" onclick="this.closest('.mobile-menu').classList.remove('open')">О школе</a>
      <a href="#features" onclick="this.closest('.mobile-menu').classList.remove('open')">Преимущества</a>
      <a href="#faq" onclick="this.closest('.mobile-menu').classList.remove('open')">FAQ</a>
      <a href="#cta" onclick="this.closest('.mobile-menu').classList.remove('open')">Записаться</a>
    </div>
  </header>

  <!-- ========================= HERO ========================= -->
  <section class="hero">
    <div class="container hero-card">
      <div>
        <span class="pill">Казахский без зубрёжки</span>
        <h1>Онлайн-уроки для детей и взрослых с <span style="background:var(--brand-light); padding:0 .4rem; border-radius:6px">Жанной</span> и ИИ-ботом</h1>
        <p class="lead">Персональная программа, разговорная практика и быстрые проверки домашки 24/7. Начните с бесплатного занятия — убедитесь сами.</p>
        <div class="badges">
          <span class="badge">40–50-минутные уроки</span>
          <span class="badge">Индивидуально</span>
          <span class="badge">Онлайн в Google Meet</span>
          <span class="badge">ИИ-бот проверяет ДЗ</span>
        </div>
        <div>
          <a class="btn btn-primary" href="#cta">Записаться на пробный урок</a>
          <a class="btn btn-ghost" href="https://instagram.com/jana_study" target="_blank" rel="noopener">Instagram</a>
        </div>
      </div>
      <div class="hero-visual">
        <!-- 🔧 Замените src на иллюстрацию Жанны (PNG/SVG). -->
        <img class="zhanne" src="./assets/janna-hero.png" alt="Жанна — персонаж Jana Study" />
        <div class="label">Знакомьтесь — Жанна 👋</div>
      </div>
    </div>
  </section>

  <!-- ========================= ABOUT / FEATURES ========================= -->
  <section id="about" class="section">
    <div class="container">
      <h2>Почему с нами удобно</h2>
      <div id="features" class="grid-3">
        <div class="card">
          <div class="icon" aria-hidden="true">📅</div>
          <h3>Гибкий график</h3>
          <p class="muted">Подстроимся под ваше расписание. Уроки онлайн, без дороги и суеты.</p>
        </div>
        <div class="card">
          <div class="icon" aria-hidden="true">🤖</div>
          <h3>ИИ-бот 24/7</h3>
          <p class="muted">Домашние задания проверяются за минуты. Экономия времени и быстрый прогресс.</p>
        </div>
        <div class="card">
          <div class="icon" aria-hidden="true">🎯</div>
          <h3>Персональная программа</h3>
          <p class="muted">Фокус на разговорной речи и ваших целях: работа, учёба, быт.</p>
        </div>
        <div class="card">
          <div class="icon" aria-hidden="true">🤝</div>
          <h3>Персональный менеджер</h3>
          <p class="muted">Всегда на связи: быстрый ответ и максимальная поддержка.</p>
        </div>
      </div>
      <p class="muted" style="margin-top:12px">Пробное занятие — 30 минут. Далее: дети — 40 минут, взрослые — 50 минут.</p>

      <div class="gallery" aria-label="Фото Жанны">
        <img src="./assets/janna-study-2.png" alt="Жанна ведёт занятие" />
        <img src="./assets/janna-with-kids.png" alt="Жанна и дети на занятии" />
        <img src="./assets/janna-office.png" alt="Жанна в деловой обстановке" />
      </div>

      <div class="spacer"></div>
      <div class="cta-strip" id="cta">
        <strong>Бесплатное пробное занятие — 30 минут</strong>
        <!-- 🔧 укажи свой номер в ссылке ниже (формат без пробелов) -->
        <a class="btn btn-primary" href="https://wa.me/77089020534?text=%D0%A1%D3%99%D0%BB%D0%B5%D0%BC!%20%D0%A5%D0%BE%D1%87%D1%83%20%D0%BF%D1%80%D0%BE%D0%B1%D0%BD%D1%8B%D0%B9%20%D1%83%D1%80%D0%BE%D0%BA%20%D0%B2%20Jana%20Study" target="_blank" rel="noopener">Написать в WhatsApp</a>
      </div>
    </div>
  </section>

  <!-- ========================= PRICING ========================= -->
  <section id="pricing" class="section">
    <div class="container">
      <h2>Стоимость</h2>
      <div class="grid-2">
        <div class="card">
          <h3>Детям</h3>
          <ul>
            <li>8 раз в месяц — <strong>43.000 тг</strong></li>
            <li>12 раз в месяц — <strong>54.000 тг</strong></li>
          </ul>
          <a class="btn btn-primary" href="https://wa.me/77089020534?text=%D0%A1%D3%99%D0%BB%D0%B5%D0%BC!%20%D1%85%D0%BE%D1%87%D1%83%20%D0%B4%D0%B5%D1%82%D1%81%D0%BA%D0%B8%D0%B5%20%D0%B7%D0%B0%D0%BD%D1%8F%D1%82%D0%B8%D1%8F%20%D0%92%20Jana%20Study" target="_blank" rel="noopener">Записаться</a>
        </div>
        <div class="card">
          <h3>Взрослым</h3>
          <ul>
            <li>8 раз в месяц — <strong>37.000 тг</strong></li>
            <li>12 раз в месяц — <strong>46.000 тг</strong></li>
          </ul>
          <a class="btn btn-primary" href="https://wa.me/77089020534?text=%D0%A1%D3%99%D0%BB%D0%B5%D0%BC!%20%D1%85%D0%BE%D1%87%D1%83%20%D0%B2%D0%B7%D1%80%D0%BE%D1%81%D0%BB%D1%8B%D0%B5%20%D0%B7%D0%B0%D0%BD%D1%8F%D1%82%D0%B8%D1%8F%20%D0%B2%20Jana%20Study" target="_blank" rel="noopener">Записаться</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================= FAQ ========================= -->
  <section id="faq" class="section">
    <div class="container">
      <h2>Частые вопросы</h2>
      <details>
        <summary>Кому подойдут занятия?</summary>
        <div class="muted">Взрослым и школьникам от 7 лет. Индивидуально, от уровня A1 до B1.</div>
      </details>
      <details>
        <summary>Как проходит урок?</summary>
        <div class="muted">Индивидуальные занятия онлайн. 70% разговорная практика и 30% нужная грамматика. Акцент на разговорном казахском. Результат после первого месяца занятий.</div>
      </details>
      <details>
        <summary>Сколько стоит?</summary>
        <div class="muted">Стоимость занятий от 37.000 тенге в месяц.</div>
      </details>
    </div>
  </section>

  <!-- ========================= FOOTER ========================= -->
  <footer>
    <div class="container foot">
      <div style="display:flex; align-items:center; gap:.8rem;">
        <picture>
          <source srcset="./assets/jana_study_logo-removebg-preview.png" type="image/png" />
          <img src="./assets/jana-study-logo.png" alt="Логотип Jana Study" width="40" height="40" />
        </picture>
        <strong>Jana Study</strong>
      </div>
      <div style="display:flex; flex-wrap: wrap; gap: 14px; align-items:center;">
        <!-- 🔧 замените номер на ваш -->
        <a href="https://wa.me/77089020534" aria-label="Написать в WhatsApp">+7 708 902 0534</a>
        <span class="muted">•</span>
        <a href="https://instagram.com/jana_study" target="_blank" rel="noopener">@jana_study</a>
      </div>
    </div>
  </footer>

  <script>
    // Плавный скролл для якорных ссылок
    document.addEventListener('click', (e) => {
      const a = e.target.closest('a[href^="#"]');
      if (!a) return;
      const id = a.getAttribute('href');
      const el = document.querySelector(id);
      if (!el) return;
      e.preventDefault();
      el.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  </script>
</body>
</html>
